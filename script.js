VanillaTilt.init(document.querySelectorAll(".tilt-card"), {
    max: 15,          // Max tilt rotation
    speed: 400,       // Speed of the effect
    glare: true,      // Adds the shiny light
    "max-glare": 0.2, // Strength of the shine
    scale: 1.05       // Zoom effect
});

console.log("System Active. Welcome, Satish.");